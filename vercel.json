{
    "buildCommand": "pnpm --filter @clawquest/shared build && pnpm --filter dashboard build",
    "outputDirectory": "apps/dashboard/dist",
    "installCommand": "pnpm install --no-frozen-lockfile",
    "framework": "vite"
}